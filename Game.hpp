#pragma once
#include <vector>
#include "Player.hpp"

using namespace std;

namespace coup {
    class Game {
        

        public:
        std:: vector<Player> _p;

        //constructer
        Game(){
          std:: vector<Player> _p;
          _p.resize(0);  
        }


        
        std::vector<string> players(){
            
            // std:: vector<string> playerss;
            // playerss.resize(this->_p.size());
            // for (unsigned long i=0; i<this->_p.size();i++){
            //     playerss[i]=this->_p.getName();
            // }
        }


        std:: palyers

    };
}